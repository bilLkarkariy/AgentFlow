########################################################################
# agentflow-worker - kind (local).
# `image.tag` is bumped by CI; everything else comes from the base file.
########################################################################

image:
  tag: sha-84586f5
config:
  DEPLOY_ENV: local
