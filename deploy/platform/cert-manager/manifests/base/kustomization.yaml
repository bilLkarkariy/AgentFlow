apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - root-ca.yaml
  - wildcard-certificate.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: agentflow-platform
      agentflow.io/component: cert-manager-config
