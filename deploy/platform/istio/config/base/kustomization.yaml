apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - gateway.yaml
  - peer-authentication.yaml
  - telemetry.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: agentflow-platform
      agentflow.io/component: istio-config
